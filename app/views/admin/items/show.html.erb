<%= image_tag @item.get_image(300,300) %>
<p>商品名<%= @item.name %></p>
<p>ジャンル<%= @item.genre.name %></p>
<p>商品説明<%= @item.introduction %></p>
<p>税込価格（税抜価格）<%= @item.with_tax_price %> (<%= @item.price.floor %>円)</p>
<%= link_to "編集する", edit_admin_item_path(@item.id), class:"btn btn-primary" %>